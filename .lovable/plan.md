## Change
Increase hero video opacity in `src/pages/Landing.tsx` from `0.6` → `0.85` so it reads as a professional, present background instead of washed-out.

Also lighten the radial overlay wash slightly so the video stays legible without darkening it — reduce overlay alpha and keep the bottom fade to the page.

No content or layout changes.